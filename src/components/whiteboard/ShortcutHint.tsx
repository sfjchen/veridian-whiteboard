/** Web keyboard shortcuts (matches original Veridian org InkCanvas). */
export function ShortcutHint() {
  return (
    <p className="shortcutHint" aria-hidden="true">
      ← pen · → eraser · Shift+move to draw/erase · ⌘Z / ⌘⇧Z / ⌘Y undo/redo
    </p>
  );
}
